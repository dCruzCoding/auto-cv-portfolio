\documentclass[a4paper,10pt]{article}

% --- PAQUETES DE DISEÑO ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[default]{lato} 
\usepackage[margin=1.25cm]{geometry} 
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{fontawesome5} 
\usepackage{titlesec}
\usepackage{enumitem}

% --- COLORES ---
\definecolor{primary}{RGB}{40, 100, 160} 
\definecolor{secondary}{RGB}{80, 80, 80} 

% --- ESTILO DE TÍTULOS ---
\titleformat{\section}
{\Large\bfseries\color{primary}}
{}{0em}
{}[\titlerule]
\titlespacing*{\section}{0pt}{1.2em}{0.6em}

% --- QUITAR NUMERACIÓN ---
\pagestyle{empty}

% --- COMIENZO DEL DOCUMENTO ---
\begin{document}

% === CABECERA ===
\begin{center}
    % APLICAMOS escape_tex AL NOMBRE Y TÍTULO
    {\Huge \textbf{\VAR{config.nombre | escape_tex}}} \\ \vspace{0.2cm}
    {\Large \textcolor{secondary}{\VAR{config.titulo_por_defecto | escape_tex}}} \\ \vspace{0.2cm}
    
    % Iconos de contacto
    % NOTA: En los enlaces (\href), el primer argumento es la URL (mejor no escapar salvo que de error),
    % el segundo argumento es el texto visible (SÍ hay que escapar).
    \small
    \BLOCK{ if config.email }
        \faEnvelope \ \href{mailto:\VAR{config.email}}{\VAR{config.email | escape_tex}} \quad | \quad
    \BLOCK{ endif }
    \BLOCK{ if config.linkedin }
        \faLinkedin \ \href{https://\VAR{config.linkedin}}{LinkedIn} \quad | \quad
    \BLOCK{ endif }
    \BLOCK{ if config.github }
        \faGithub \ \href{https://\VAR{config.github}}{GitHub} \quad | \quad
    \BLOCK{ endif }
    \BLOCK{ if config.web }
        \faGlobe \ \href{https://\VAR{config.web}}{Portfolio}
    \BLOCK{ endif }
\end{center}

\vspace{0.3cm}

% === PERFIL ===
\BLOCK{ if config.perfil }
\section*{Perfil}
% Aquí aplicamos el filtro al texto del perfil
\VAR{config.perfil | escape_tex}
\BLOCK{ endif }

% === EXPERIENCIA ===
\section*{Experiencia Laboral}
\begin{itemize}[leftmargin=0cm, label={}]
    \BLOCK{ for item in experiencia }
    \item 
        % Aplicamos filtro a puesto, fecha, empresa, ubicación y descripción
        \textbf{\large \VAR{item.puesto | escape_tex}} \hfill \textbf{\VAR{item.fecha | escape_tex}} \\
        \textcolor{primary}{\textit{\VAR{item.empresa | escape_tex}}} \ \textbullet \ \textit{\VAR{item.ubicacion | escape_tex}} \\
        \vspace{0.05cm}
        \VAR{item.descripcion | escape_tex}
        \vspace{0.3cm}
    \BLOCK{ endfor }
\end{itemize}

% === FORMACIÓN ===
\BLOCK{ if formacion }
\section*{Formación Académica}
\begin{itemize}[leftmargin=0cm, label={}]
    \BLOCK{ for item in formacion }
    \item 
        \textbf{\VAR{item.titulo | escape_tex}} \hfill \VAR{item.fecha | escape_tex} \\
        \textit{\VAR{item.centro | escape_tex}}
        \vspace{0.15cm}
    \BLOCK{ endfor }
\end{itemize}
\BLOCK{ endif }

% === HABILIDADES ===
\BLOCK{ if habilidades }
\section*{Habilidades y Herramientas}
\begin{itemize}[leftmargin=0cm, label={}]
    \item 
    \BLOCK{ for item in habilidades }
        % Filtro también aquí por si pones "C++" o "C#"
        \colorbox{primary!10}{\textcolor{black}{\textbf{\VAR{item.nombre | escape_tex}}}} \quad
    \BLOCK{ endfor }
\end{itemize}
\BLOCK{ endif }

\end{document}